let usuario = parseInt(prompt ("¿Cual es tu edad?"));
    if (usuario >= 18) {
        alert ("eres mayor de edad, ya eres legal");
    }
    else {
        alert ("todavia estas chiquito, crece un poquito más");
    }
